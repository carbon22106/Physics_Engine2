from settings import *
from ball import *

class PrintAndDebug:
    def __init__(self):
        ...
    
    def ball_prints(self, ball_name):
        # self.debug_ball = ball_name
        # print(f"KE : {round((1/2)*(self.debug_ball.ball_mass)*(self.debug_ball.ball_vel.length_squared()), 2)}", end=' ')
        # print(f"PE : {round((self.debug_ball.ball_mass)*(self.debug_ball.GRAVITY)*(SCREEN_HEIGHT - self.debug_ball.ball.centery), 2)}")
        ...